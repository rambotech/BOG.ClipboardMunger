<?xml version="1.0"?>
<ClipboardMungerScriptContainer xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema">
  <Title>Hex View</Title>
  <Description>Translate the clipboard content to a Hex View for content analysis</Description>
  <Script>// Change the clipboard content to a Hex View

using System;
using System.Text;

public class Script : Interfaces.IClipboard
{
	public string Munge (string clipboardSource)
	{
		StringBuilder s = new StringBuilder();
		if (clipboardSource.Length &gt; 0)
		{
			for (int iRow = 0; iRow &lt;= ((clipboardSource.Length - 1) / 16); iRow++)
			{
				s.Append (String.Format("{0:x4}: ", iRow * 16));
				for (int iCol = 0; iCol &lt; 16; iCol++)
				{
					if (clipboardSource.Length &gt; iRow * 16 + iCol)
					{
						byte b = (byte) clipboardSource[iRow * 16 + iCol];
						s.Append (String.Format("{0:x2} ", b));
					}
					else
					{
						s.Append ("   ");  // 3 spaces
					}
				}
				s.Append (" | ");
				for (int iCol = 0; iCol &lt; 16; iCol++)
				{
					if (clipboardSource.Length &gt; iRow * 16 + iCol)
					{
						byte b = (byte) clipboardSource[iRow * 16 + iCol];
						s.Append ((b &lt; 32 || b &gt; 128) ? '.' : (char) b);
					}
					else
					{
						s.Append (' ');  // 1 space
					}
				}
				s.Append ("\r\n");
			}
		}
		return s.ToString();
	}
}
</Script>
  <Language>1</Language>
  <TestData>test test test test test test test test test 
test test test test test test test test test </TestData>
  <References>
    <string>System, Version=2.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</string>
  </References>
  <OtherReferences />
</ClipboardMungerScriptContainer>